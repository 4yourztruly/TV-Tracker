export function Header() {
  return (
    <header className="sticky top-0 z-10 flex items-center justify-center border-b border-ink-800 bg-ink-950/95 px-4 py-3 backdrop-blur">
      <h1 className="font-[var(--font-display)] text-lg font-semibold tracking-tight text-ink-100">
        TV Tracker
      </h1>
    </header>
  );
}
